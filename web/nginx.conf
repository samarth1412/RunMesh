server {
  listen 3000;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri /index.html; }
  location /v1/ { proxy_pass http://control-plane:8080; proxy_set_header Host $host; }
  location /health/ { proxy_pass http://control-plane:8080; }
}
